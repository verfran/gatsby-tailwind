import * as React from "react"
import { Link } from "gatsby"
import { StaticImage } from "gatsby-plugin-image"

const IndexPage = () => (
    <h1 className="mb-5 flex justify-center font-semibold text-blue-700">
        test heading
    </h1>
)

export default IndexPage
